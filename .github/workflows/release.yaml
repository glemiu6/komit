# .github/workflows/release.yaml
name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  # ----------------------------
  # Build binaries
  # ----------------------------
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            name: komit-linux-x86_64
          - os: macos-latest
            name: komit-macos-arm64
          - os: windows-latest
            name: komit-windows-x86_64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: pip install pyinstaller ollama httpx platformdirs requests rich argcomplete

      - name: Build binary
        run: pyinstaller --onefile komit/main.py --name ${{ matrix.name }}

      - name: Upload artifact (Unix)
        if: runner.os != 'Windows'
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.name }}
          path: dist/${{ matrix.name }}

      - name: Upload artifact (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.name }}
          # Fix: PyInstaller adds .exe automatically on Windows
          path: dist/${{ matrix.name }}.exe

  # ----------------------------
  # Publish to PyPI
  # ----------------------------
  publish-pypi:
    name: Publish to PyPI
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Install dependencies and build
        run: |
          uv sync --dev
          uv run python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}

  # ----------------------------
  # Create GitHub Release + attach binaries
  # ----------------------------
  github-release:
    name: GitHub Release
    # Fix: now depends on build job in the SAME workflow
    # so artifacts are available to download
    needs: [build, publish-pypi]
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts/

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          files: artifacts/**/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}